dofile("LuaScript.entity.entity")

local _M = {
    id = 0, --固有属性
    name = "",  --用于在界面中显示
    uniqueName = "这个会动态生成，最终的结构为    用户uid + 卡牌id + 随机生成一个字母", --程序所使用的id
    skills = {

    },
    hp = 0,
    maxHp = 0,
    mp = 0,
    maxMp = 0
}

-- 传入的是一个由json字符串转出来的对象
function _M:Create(data)
    local card = {}
    setmetatable(card, self)
    card.__index = self
    -- 有一个被动技能配置表
    card.skills = {
        asd = 1,
        zxc = 2
    }
    --@RefType [LuaScript.cards.alice#_M]
    return card
end

function _M:GetId()
    return self.id
end

function _M:GetUniqueName()
    --这个id由服务器来分配
    return self.uniqueName
end

function _M:GetName()
    return self.name
end

function _M:GetSkill(index)
    
end

return _M