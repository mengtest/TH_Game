#include <string>


class GamePlay
{
private:
    /* data */
public:
    GamePlay(/* args */);
    
    ~GamePlay();

    double call(double x, double y);

    const char * func(const std::string& name);
};
